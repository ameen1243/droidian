name: Build Droidian Kernel
on: [workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4 # تحديث للنسخة 4
      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential flex bison libssl-dev libelf-dev
      - name: Build Kernel
        run: |
          chmod +x build_kernel.sh
          ./build_kernel.sh
      - name: Upload Artifact
        uses: actions/upload-artifact@v4 # التحديث المطلوب للنسخة 4
        with:
          name: Droidian-Kernel-A21s
          path: out/*.zip
          
